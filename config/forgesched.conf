# ForgeSched Configuration File

# Server settings
server.port=8080
server.io_threads=2
server.worker_threads=8

# Logging configuration
log.level=INFO
log.dir=./logs

# Database settings
database.path=./data/forgesched.db

# Scheduler settings
scheduler.max_running_tasks=32
scheduler.task_timeout_sec=10800

# Worker settings
worker.heartbeat_timeout_sec=30
